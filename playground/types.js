/* eslint-disable */
const name = "David";
const middle = 'mo';
const last = `Mofty`;

const sentence = 'She\'s so cool';
const sentenceTwo = `She's so "cool"
I'll probably write
A song
`;

const sentenceFull = `hello my name is ${name}
I am ${1 + 44}
years old
`;

const html = `
<div>
  <h2>${name}</h2>
  <p>${sentenceFull}</p>
</div>
`;
